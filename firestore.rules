rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 🔐 Basic authenticated read/write access
      allow read, write: if request.auth != null;
    }
  }
}
